# Define a function named unique that takes a list as input, with a default empty list if no argument is provided.
def unique(list=[]):
    # Initialize an empty list to store unique elements.
    new_list = []
    # Iterate through each item in the input list.
    for item in list:
        # Check if the item is not already in the new_list.
        if item not in new_list:
            # If the item is not in new_list, append it to new_list.
            new_list.append(item)
    # Return the list containing unique elements.
    return new_list

# Call the unique function with arguments 1, 1, 4, 5, 1 and print the result.
print(unique([1, 1, 4, 5, 1]))
